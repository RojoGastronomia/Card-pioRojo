<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Erro no Envio do Boleto - SiteCard</title>
    <style>
        body {
            background: #f3f4f6;
            font-family: 'Segoe UI', Arial, sans-serif;
            margin: 0;
            padding: 0;
            color: #111;
        }
        .container {
            max-width: 600px;
            margin: 32px auto;
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 4px 24px 0 rgba(220,53,69,0.10);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(90deg, #dc3545 60%, #f87171 100%);
            color: #fff;
            padding: 32px 24px 20px 24px;
            text-align: center;
        }
        .header h1 {
            margin: 0 0 8px 0;
            font-size: 2rem;
            letter-spacing: 1px;
        }
        .header p {
            margin: 0;
            font-size: 1.1rem;
            opacity: 0.95;
        }
        .content {
            padding: 32px 24px 16px 24px;
            color: #111;
        }
        .error-box {
            background: #fee2e2;
            border-radius: 12px;
            box-shadow: 0 2px 8px 0 rgba(220,53,69,0.06);
            padding: 24px;
            margin-bottom: 24px;
            text-align: center;
            border: 1px solid #fca5a5;
        }
        .error-box h2 {
            color: #991b1b;
            margin: 0 0 12px 0;
        }
        .error-box p {
            color: #7f1d1d;
            margin: 8px 0;
        }
        .details {
            background: #f8f9fa;
            border-radius: 12px;
            box-shadow: 0 2px 8px 0 rgba(37,99,235,0.06);
            padding: 24px;
            margin-bottom: 24px;
            border: 1px solid #e9ecef;
        }
        .details h2 {
            margin-top: 0;
            color: #dc2626;
            font-size: 1.2rem;
        }
        .details p {
            margin: 8px 0;
            font-size: 1rem;
            color: #111;
        }
        .customer-box {
            background: #dbeafe;
            border-radius: 12px;
            padding: 18px 20px;
            margin-bottom: 24px;
            border: 1px solid #93c5fd;
        }
        .customer-box h3 {
            margin: 0 0 8px 0;
            color: #1e40af;
            font-size: 1.1rem;
        }
        .customer-box p {
            margin: 6px 0;
            font-size: 0.98rem;
            color: #1e3a8a;
        }
        .error-details {
            background: #f8fafc;
            border: 1px solid #fca5a5;
            border-radius: 8px;
            padding: 15px;
            margin: 20px 0;
            font-family: monospace;
            font-size: 12px;
            color: #7f1d1d;
        }
        .error-details h4 {
            color: #991b1b;
            margin: 0 0 8px 0;
        }
        .action-box {
            background: #fef3c7;
            border: 1px solid #fde047;
            border-radius: 12px;
            padding: 18px 20px;
            margin-bottom: 24px;
        }
        .action-box h3 {
            color: #92400e;
            margin: 0 0 8px 0;
        }
        .action-box ol, .action-box ul {
            color: #78350f;
            margin: 0 0 10px 18px;
            padding: 0;
        }
        .action-box p {
            color: #78350f;
            margin: 8px 0;
        }
        .admin-link {
            display: inline-block;
            background: linear-gradient(90deg, #dc3545 60%, #f87171 100%);
            color: #fff;
            font-weight: bold;
            padding: 12px 28px;
            border-radius: 8px;
            text-decoration: none;
            font-size: 1.05rem;
            margin-top: 10px;
            box-shadow: 0 2px 8px 0 rgba(220,53,69,0.10);
            transition: background 0.2s;
        }
        .admin-link:hover {
            background: linear-gradient(90deg, #f87171 60%, #dc3545 100%);
        }
        .footer {
            background: #f8f9fa;
            color: #495057;
            text-align: center;
            padding: 18px 12px;
            font-size: 0.98rem;
            border-top: 1px solid #dee2e6;
        }
        @media (max-width: 600px) {
            .container, .content, .header, .footer {
                padding-left: 6px !important;
                padding-right: 6px !important;
            }
            .details, .error-box, .customer-box, .error-details, .action-box {
                padding: 12px 8px !important;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>❌ Erro no Envio do Boleto</h1>
            <p>Falha ao enviar boleto para o cliente</p>
        </div>
        <div class="content">
            <div class="error-box">
                <h2>⚠️ Problema Detectado</h2>
                <p><strong>O boleto para o pedido #{{orderId}} não foi enviado com sucesso para o cliente.</strong></p>
                <p>É necessário verificar o problema e reenviar o boleto manualmente.</p>
            </div>
            <div class="details">
                <h2>📋 Detalhes do Pedido #{{orderId}}</h2>
                <p><strong>Evento:</strong> {{eventName}}</p>
                <p><strong>Data do Evento:</strong> {{eventDate}}</p>
                <p><strong>Local:</strong> <span style="color:#1e40af;font-weight:600">{{location}}</span></p>
                <p><strong>Valor Total:</strong> <span style="color:#dc2626;font-weight:600">R$ {{totalAmount}}</span></p>
                <p><strong>Data da Tentativa de Envio:</strong> {{attemptDate}}</p>
            </div>
            <div class="customer-box">
                <h3>👤 Cliente Afetado</h3>
                <p><strong>Nome:</strong> {{customerName}}</p>
                <p><strong>Email:</strong> {{customerEmail}}</p>
                <p><strong>Status do Email:</strong> <span style="color: #dc2626;">❌ Falha no envio</span></p>
            </div>
            <div class="error-details">
                <h4>🔍 Detalhes do Erro:</h4>
                <p><strong>Erro:</strong> {{errorMessage}}</p>
                <p><strong>Código:</strong> {{errorCode}}</p>
                <p><strong>Timestamp:</strong> {{errorTimestamp}}</p>
            </div>
            <div class="action-box">
                <h3>🛠️ Ação Necessária</h3>
                <ol>
                    <li>Verifique se o email do cliente está correto</li>
                    <li>Teste o envio manual do boleto</li>
                    <li>Se necessário, entre em contato direto com o cliente</li>
                    <li>Atualize o status do pedido no sistema</li>
                </ol>
                <a href="{{adminPanelUrl}}" class="admin-link" target="_blank">🔗 Acessar Painel Administrativo</a>
            </div>
            <div class="action-box">
                <h3>📧 Alternativas de Contato</h3>
                <ul>
                    <li><strong>Email direto:</strong> {{customerEmail}}</li>
                    <li><strong>Telefone:</strong> {{customerPhone}}</li>
                    <li><strong>WhatsApp:</strong> {{customerWhatsApp}}</li>
                </ul>
                <p><strong>Sugestão:</strong> Entre em contato direto com o cliente para confirmar o email correto e reenviar o boleto.</p>
            </div>
            <p style="margin-top:18px;color:#111;"><strong>Importante:</strong> Este erro pode afetar o pagamento do pedido. Resolva o problema o mais rápido possível para não impactar o evento.</p>
        </div>
        <div class="footer">
            <p>Este é um email automático, por favor não responda.<br>© 2024 SiteCard. Todos os direitos reservados.</p>
        </div>
    </div>
</body>
</html> 